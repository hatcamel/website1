---
const title = "Luke 8:17";
const description = "For there is nothing hidden that will not be disclosed, and nothing concealed that will not be known or brought out into the open.";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Luke 8:17</title>
    <meta name="description" content="For there is nothing hidden that will not be disclosed, and nothing concealed that will not be known or brought out into the open." />
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🌟</text></svg>" />
    <script is:inline src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        font-family: 'Georgia', serif;
        color: #e0e0e0;
        background: #000;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .hero {
        position: relative;
        width: 100%;
        min-height: 100vh;
        background-image: url('https://www.shutterstock.com/shutterstock/videos/1070469679/thumb/5.jpg?ip=x480'); 
        background-size: cover;
        background-position: center;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
      }
      .overlay {
        background: rgba(0, 0, 0, 0.65);
        padding: 3rem;
        border-radius: 1rem;
        max-width: 90%;
      }
      .reference {
        font-size: 4.5rem;
        font-weight: bold;
        letter-spacing: 0.15em;
        margin-bottom: 2rem;
        background: linear-gradient(to right, #ffd700, #ffaa00);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 30px rgba(255, 215, 0, 0.4);
      }
      .quote {
        font-size: 2.5rem;
        line-height: 1.6;
        font-style: italic;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8);
      }
      .glow {
        animation: subtle-pulse 6s infinite;
      }
      @keyframes subtle-pulse {
        0%, 100% { opacity: 0.95; }
        50% { opacity: 1; text-shadow: 0 0 40px rgba(255, 215, 0, 0.6); }
      }
      @media (min-width: 768px) {
        .reference { font-size: 7rem; }
        .quote { font-size: 4rem; }
        .overlay { padding: 4rem; }
      }
    </style>
  </head>
  <body>
    <div class="hero">
      <!-- Dark overlay + text container -->
      <div class="overlay glow">
        <h1 class="reference">LUKE 8:17</h1>
        <blockquote class="quote">
          For there is nothing hidden that will not be disclosed,<br>
          and nothing concealed that will not be known<br>
          or brought out into the open.
        </blockquote>
      </div>
    </div>
  </body>
</html>
   
